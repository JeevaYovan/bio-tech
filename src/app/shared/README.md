# `src/app/shared/`

Custom components live here ONLY when `@klaro/ui` genuinely lacks the
primitive (per PROMPT.md §5 and CLAUDE.md). Each entry below justifies
why the custom component exists rather than reusing Klaro.

| Component | Justification | Klaro alternatives considered |
|---|---|---|
| _none yet_ | | |

When adding an entry, also create an ADR via `/adr-new` recording the
decision. The ADR captures the long-form context; this table is the
quick index.
